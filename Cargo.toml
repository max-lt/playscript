[package]
name = "playscript"
version = "1.1.0"
edition = "2024"

[dependencies]

# The engine stays at the repo root and dependency-free. The wasm bridge is
# a workspace member that depends on it and owns the JSON serialization.
[workspace]
members = ["wasm"]
